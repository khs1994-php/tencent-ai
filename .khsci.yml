workspace:
  base: /src
  path: .

pipeline:
  test:
    image: khs1994/php-fpm:7.2.6-alpine3.7
    commands:
      - pwd
      - composer install -q
      - composer update -q
      - vendor/bin/phpunit
